import { title } from "process";

export const metadata = {
    // title: "Products Page"
    title: {
      absolute: "BLOG",
    }
};

const Page = () => {
  return <div>Products Page</div>;
};

export default Page;
